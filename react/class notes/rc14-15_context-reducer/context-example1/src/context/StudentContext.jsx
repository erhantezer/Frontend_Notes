import { createContext } from "react";

//! We create a context for student object and export for future usage
export const StudentContext = createContext();

//* studentsContext yerine herhangi bir isim verilir.
